# Data

Place local datasets here only for your own experiments.

Do not commit raw datasets or generated dataset exports. The training config currently resolves paths from `Pytorch3D/yolov5`, so either place data there or use relative paths such as `../../data/carla_dataset` in `configs/default.yaml`.
